server:
  port: 8080
  servlet:
    context-path: /api/v1
spring:
  datasource:
    url: jdbc:h2:mem:superheroes_db
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none